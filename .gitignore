node_modules
.next
out
.vercel
.DS_Store
src/generated
.wrangler
.dev.vars
worker/assets
.env*.local

.playwright-mcp
tsconfig.tsbuildinfo

# ai/ — uv's own venv, the one pywrangler builds for the Worker, and the vendored packages
ai/.venv
ai/.venv-workers
ai/python_modules
__pycache__
.pytest_cache

# Playwright
test-results
playwright-report
blob-report
.playwright

# Next regenerates this on every build, with different contents for `next dev` and
# `next build`, so a tracked copy is dirty half the time. It is generated, not authored.
next-env.d.ts
